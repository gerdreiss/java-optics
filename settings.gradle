pluginManagement {
    plugins {
        id 'com.diffplug.spotless' version '5.10.0'
    }
}

plugins {
    id 'com.diffplug.spotless' apply false
}

rootProject.name = 'java-optics'

include 'core'
include 'interop-vavr'

